rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /wordcloud_words/{document} {
      // 影片示範用白名單集合，之後若改成正式服務要收斂權限。
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
